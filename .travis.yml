language: python

python:
  - "2.7"

env:
  matrix:
    - CXX=clang++ CC=clang
    - CXX=g++ CC=gcc

before_script:
  - sudo apt-get -qq update
  - sudo apt-get -qq install graphviz

install:
  - python setup.py install

script:
  - python setup.py test
  - python setup.py doc
