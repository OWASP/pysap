language: python

python:
  - "2.7"

compiler:
  - clang
  - gcc

before_script:
  - sudo apt-get -qq update
  - sudo apt-get -qq install graphviz

install:
  - python setup.py install

script:
  - python setup.py test
  - python setup.py doc
